# === REAL DATA (never push) ===
data/real_backup/

# === ALLOW ONLY DUMMY DATA FILES ===
!data/raw/city/*dummy*
!data/raw/hospitals/*dummy*

# Keep folder structure so GitHub Actions doesn’t break
!data/
!data/raw/
!data/raw/city/
!data/raw/hospitals/

# Block everything else inside raw (real files)
data/raw/*

# === MODELS (large files — never push) ===
models_tff/*.keras

# Keep the folder & gitkeep
!models_tff/
!models_tff/.gitkeep

# === PYTHON CACHE ===
__pycache__/
*.pyc
*.pyo
.ipynb_checkpoints/

# === ENVIRONMENTS ===
.venv/
venv/
env/

# === OS JUNK ===
.DS_Store
Thumbs.db
